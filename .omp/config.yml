# Project-scoped Oh My Pi settings.
# Overrides the global approval mode for this repository only.
tools:
  approvalMode: yolo
skills:
  enabled: true
  enableSkillCommands: true
mcp:
  enableProjectConfig: true
