import { FormEvent } from "react"; import Composer from "./Composer"; import LegalMark from "./LegalMark";
type WelcomeProps={question:string;suggestions:string[];onQuestion:(value:string)=>void;onSubmit:(event:FormEvent<HTMLFormElement>)=>void};
export default function Welcome({question,suggestions,onQuestion,onSubmit}:WelcomeProps){return <div className="welcome"><LegalMark className="welcome-mark"/><h1>Hỏi đáp pháp luật giao thông</h1><p>Hỏi rõ điều bạn cần biết. Mỗi câu trả lời đều kèm căn cứ để kiểm tra.</p><Composer id="question" value={question} hero onChange={onQuestion} onSubmit={onSubmit}/><div className="prompt-chips" aria-label="Gợi ý">{suggestions.map(item=><button key={item} type="button" onClick={()=>onQuestion(item)}>{item}</button>)}</div></div>}
