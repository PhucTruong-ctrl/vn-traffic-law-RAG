"use client";
export type Citation = { provision_id?: string; document_number?: string; article?: string; source_url?: string; source_text?: string; page?: number | string; bbox?: number[] };
export default function CitationCard({ citation, onOpenSource }: { citation: Citation; onOpenSource: (citation: Citation) => void }) { return <article className="citation-card"><strong>{citation.document_number || citation.provision_id || "Quy định liên quan"}</strong>{citation.article && <span> · {citation.article}</span>}<div className="citation-meta">{citation.page != null && <span>Trang {citation.page}</span>}{citation.bbox?.length === 4 && <span> · Vị trí văn bản</span>}</div><button type="button" onClick={() => onOpenSource(citation)}>Xem đoạn trích</button>{citation.source_url && <a href={citation.source_url} target="_blank" rel="noreferrer">Mở nguồn</a>}</article>; }
